<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover">
  <title>✈ Aviator · premium</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&display=swap');
    *{margin:0;padding:0;box-sizing:border-box;-webkit-tap-highlight-color:transparent}
    body{font-family:'Nunito',sans-serif;background:#0b0f19;display:flex;justify-content:center;align-items:flex-start;min-height:100vh}
    .app{width:100%;max-width:430px;background:#141a2b;min-height:100vh;display:flex;flex-direction:column;overflow-x:hidden;transition:filter .2s;box-shadow:0 0 0 1px #1f2a3f;}

    /* DRAWER */
    .ov{position:fixed;inset:0;background:rgba(0,0,0,.85);z-index:200;display:flex;align-items:flex-start;justify-content:flex-end;opacity:0;pointer-events:none;transition:opacity .25s;backdrop-filter:blur(2px);}
    .ov.open{opacity:1;pointer-events:all}
    .drawer{width:82%;max-width:300px;background:#1e293b;min-height:100vh;transform:translateX(100%);transition:transform .3s cubic-bezier(0.2,0.9,0.4,1);display:flex;flex-direction:column;box-shadow:-4px 0 20px rgba(0,0,0,.5);}
    .ov.open .drawer{transform:translateX(0)}
    .dh{background:#0f172a;padding:16px 18px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #334155}
    .dh-t{color:#fff;font-size:16px;font-weight:800;letter-spacing:-0.3px}
    .dh-x{color:#94a3b8;font-size:24px;cursor:pointer;padding:4px;transition:color .2s}
    .dh-x:hover{color:#f1f5f9}
    .db{padding:14px 12px;flex:1;overflow-y:auto}
    .ds{margin-bottom:22px}
    .ds-t{color:#64748b;font-size:10.5px;font-weight:800;letter-spacing:1.5px;text-transform:uppercase;margin-bottom:10px;padding-left:4px}
    .di{display:flex;align-items:center;justify-content:space-between;padding:12px 14px;background:#0f172a;border-radius:14px;margin-bottom:6px;cursor:pointer;transition:all .15s;border:1px solid #1e293b}
    .di:hover{background:#1e293b;border-color:#3b82f6}
    .di-l{display:flex;align-items:center;gap:12px}
    .di-ic{font-size:18px;width:28px;text-align:center}
    .di-lb{color:#f8fafc;font-size:13.5px;font-weight:700}
    .di-sb{color:#64748b;font-size:11px;font-weight:600}
    .sr{display:flex;align-items:center;gap:12px;padding:8px 0}
    .sl-lb{color:#cbd5e1;font-size:12px;font-weight:600;width:65px}
    input[type=range]{flex:1;accent-color:#3b82f6;height:4px;background:#1e293b}
    .ts{width:42px;height:24px;background:#334155;border-radius:24px;position:relative;cursor:pointer;transition:background .2s;flex-shrink:0}
    .ts.on{background:#3b82f6}
    .ts::after{content:'';position:absolute;top:3px;left:3px;width:18px;height:18px;background:#fff;border-radius:50%;transition:left .2s;box-shadow:0 1px 3px rgba(0,0,0,.3)}
    .ts.on::after{left:21px}

    /* MODAL */
    .mo{position:fixed;inset:0;background:rgba(0,0,0,.8);z-index:300;display:flex;align-items:center;justify-content:center;opacity:0;pointer-events:none;transition:opacity .2s;backdrop-filter:blur(3px);}
    .mo.open{opacity:1;pointer-events:all}
    .md{background:#1e293b;border-radius:24px;width:90%;max-width:340px;overflow:hidden;transform:scale(.92);transition:transform .2s;box-shadow:0 20px 40px rgba(0,0,0,.6);border:1px solid #334155}
    .mo.open .md{transform:scale(1)}
    .mh{background:#0f172a;padding:16px 18px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #334155}
    .mh-t{color:#fff;font-size:16px;font-weight:800}
    .mh-x{color:#94a3b8;font-size:22px;cursor:pointer}
    .mb{padding:18px}
    .mt{display:flex;background:#0f172a;border-radius:14px;padding:4px;margin-bottom:16px}
    .mt-b{flex:1;text-align:center;padding:9px;border-radius:12px;color:#94a3b8;font-size:13.5px;font-weight:700;cursor:pointer;transition:all .15s}
    .mt-b.active{background:#1e293b;color:#fff;box-shadow:0 2px 6px #0000001a}
    .mbal{text-align:center;margin-bottom:16px;padding:12px;background:#0f172a;border-radius:16px}
    .mbal span{color:#4ade80;font-size:22px;font-weight:900}
    .mbal small{color:#64748b;font-size:11px}
    .mlb{color:#94a3b8;font-size:11px;font-weight:700;margin-bottom:6px;display:block}
    .mi{width:100%;background:#0f172a;border:1.5px solid #334155;border-radius:12px;padding:12px 14px;color:#fff;font-size:15px;font-weight:700;outline:none;margin-bottom:12px}
    .mi:focus{border-color:#3b82f6}
    .qd{display:flex;gap:8px;margin-bottom:14px}
    .qdb{flex:1;background:#0f172a;border:1.5px solid #334155;border-radius:10px;color:#cbd5e1;font-size:12px;font-weight:700;padding:8px 2px;text-align:center;cursor:pointer}
    .qdb:hover{border-color:#3b82f6;color:#fff}
    .mab{width:100%;padding:14px;border-radius:14px;border:none;font-size:15px;font-weight:800;cursor:pointer}
    .mab.dep{background:#3b82f6;color:#fff;box-shadow:0 6px 14px #1e3a8a80}
    .mab.wd{background:#f97316;color:#fff;box-shadow:0 6px 14px #9a341280}

    /* header */
    .th{background:#0f172a;display:flex;align-items:center;justify-content:space-between;padding:10px 16px;border-bottom:1px solid #1e293b}
    .logo{font-size:22px;font-weight:900;color:#ef4444;font-style:italic;letter-spacing:-0.5px}
    .hr{display:flex;align-items:center;gap:10px}
    .bp{display:flex;align-items:center;gap:4px;background:#1e293b;border-radius:40px;padding:5px 12px;cursor:pointer;border:1px solid #334155}
    .ba{color:#4ade80;font-weight:800;font-size:14px}
    .bc{color:#94a3b8;font-size:11px;font-weight:700}
    .dep-tag{background:#f97316;color:#000;font-size:9px;font-weight:900;padding:3px 7px;border-radius:20px;margin-left:6px}
    .hi{color:#94a3b8;font-size:24px;cursor:pointer;padding:4px}

    .sb{background:#0b1320;display:flex;justify-content:space-between;padding:10px 16px;border-bottom:1px solid #1e293b}
    .fb{color:#cbd5e1;font-size:12px;font-weight:700}
    .bfl{color:#f8fafc;font-weight:800}
    .arb{width:30px;height:30px;background:#f97316;border-radius:50%;display:flex;align-items:center;justify-content:center;color:#000;font-size:18px;font-weight:900}

    .svt{background:#0f172a;display:flex;border-bottom:1px solid #1e293b}
    .st{flex:1;padding:10px 2px;text-align:center;color:#64748b;font-size:11px;font-weight:700;cursor:pointer;border-bottom:3px solid transparent}
    .st.active{color:#fff;border-bottom-color:#3b82f6}
    .sdo{width:8px;height:8px;border-radius:50%;display:inline-block;margin-right:4px}

    .hs{background:#0b1320;display:flex;gap:6px;padding:8px 14px;overflow-x:auto;border-bottom:1px solid #1e293b}
    .hv{font-size:11px;font-weight:800;padding:3px 8px;border-radius:20px}
    .hv.g{color:#4ade80;background:#14532d80}
    .hv.b{color:#60a5fa;background:#1e3a8a80}
    .hv.r{color:#f87171;background:#7f1d1d80}
    .hv.o{color:#fb923c;background:#9a341280}
    .hv.p{color:#c084fc;background:#4c1d9580}

    .ga{background:#0a0f18;position:relative;flex-shrink:0}
    #gc{width:100%;height:230px;display:block}
    .md-disp{position:absolute;top:18px;left:50%;transform:translateX(-50%);font-size:48px;font-weight:900;color:#fff;text-shadow:0 0 20px #3b82f6;pointer-events:none}
    .md-disp.flying{animation:gp .8s infinite}
    .md-disp.crashed{color:#ef4444;animation:shake .08s 8}
    .wo{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;pointer-events:none}
    .wl{color:#94a3b8;font-size:13px}
    .cd{color:#facc15;font-size:30px;font-weight:900}

    .tk{background:#0f172a;border-top:1px solid #1e293b;height:26px;overflow:hidden}
    .tki{display:flex;gap:20px;animation:tka 18s linear infinite;white-space:nowrap;padding:0 12px;align-items:center;height:100%}
    .tki-i{color:#94a3b8;font-size:10.5px;font-weight:600}
    .tn{color:#60a5fa}.tw{color:#4ade80}.tm{color:#f97316}

    .pb{background:#0b1320;display:flex;align-items:center;justify-content:space-between;padding:8px 14px;border-top:1px solid #1e293b}
    .ulb{background:#ef4444;color:#fff;font-size:16px;font-weight:900;padding:2px 8px;border-radius:4px}
    .sp{display:flex;gap:6px;background:#1e293b;padding:4px 8px;border-radius:12px}
    .spn{color:#fff;font-size:11px;font-weight:800}

    .pf{background:#0b1320;padding:6px 14px;min-height:48px;max-height:48px;overflow:hidden;border-top:1px solid #1e293b}
    .fr{display:flex;align-items:center;gap:6px;font-size:10.5px;color:#94a3b8;animation:su .2s}
    .fn{color:#93c5fd}.fa{color:#fbbf24}.fw{color:#4ade80}.fm{color:#f97316}

    .bpn{background:#0f172a;border-radius:20px 20px 0 0;padding:16px 14px;flex:1}
    .mto{display:flex;background:#1e293b;border-radius:30px;padding:4px;margin-bottom:14px}
    .mtob{flex:1;padding:8px;border-radius:26px;font-weight:800;font-size:13px;color:#94a3b8;cursor:pointer}
    .mtob.active{background:#334155;color:#fff}
    .bsl{background:#1a2536;border-radius:18px;padding:14px;margin-bottom:12px;border:1px solid #2d3a4e}
    .smr{display:flex;justify-content:space-between;margin-bottom:10px}
    .smto{background:#0f172a;border-radius:20px;padding:2px}
    .smb{padding:6px 16px;border-radius:18px;font-size:12px;font-weight:700;color:#94a3b8;cursor:pointer}
    .smb.active{background:#1e293b;color:#fff}
    .srm{width:30px;height:30px;background:#1e293b;border-radius:10px;display:flex;align-items:center;justify-content:center;color:#cbd5e1;font-size:20px;cursor:pointer}
    .sir{display:flex;align-items:center;gap:10px;margin-bottom:10px}
    .mb2,.pb2{width:36px;height:36px;background:#1e293b;border-radius:50%;border:none;color:#fff;font-size:22px;cursor:pointer}
    .ai{flex:1;background:#0f172a;border-radius:14px;padding:10px;color:#fff;font-size:16px;font-weight:800;text-align:center;border:1.5px solid #334155}
    .qa{display:grid;grid-template-columns:repeat(4,1fr);gap:6px;margin-bottom:12px}
    .qa-b{background:#0f172a;border-radius:10px;padding:8px 0;color:#cbd5e1;font-size:12px;font-weight:700;text-align:center;cursor:pointer;border:1px solid #334155}
    .qa-b.sel{border-color:#3b82f6;color:#fff;background:#1e3a8a40}
    .bmb{width:100%;padding:14px;border-radius:16px;border:none;font-weight:800;font-size:16px;display:flex;flex-direction:column;gap:2px}
    .bmb.bet-st{background:#3b82f6;color:#fff}
    .bmb.can-st{background:#334155;color:#cbd5e1}
    .bmb.co-st{background:#f97316;color:#fff;animation:cop .7s infinite}
    .bsub{font-size:11px;opacity:.9}

    /* Toast notification */
    .toast{position:fixed;bottom:80px;left:50%;transform:translateX(-50%);background:#1e293b;color:#fff;padding:12px 20px;border-radius:40px;font-weight:700;box-shadow:0 8px 20px #0000004d;z-index:400;border:1px solid #334155;opacity:0;transition:opacity .2s;pointer-events:none;white-space:nowrap}
    .toast.show{opacity:1}
    .toast.success{border-left:4px solid #4ade80}
    .toast.warning{border-left:4px solid #f97316}

    @keyframes gp{0%,100%{text-shadow:0 0 20px #3b82f6}50%{text-shadow:0 0 50px #60a5fa}}
    @keyframes cop{0%,100%{box-shadow:0 4px 15px #ea580c}50%{box-shadow:0 4px 25px #f97316;transform:scale(1.01)}}
    @keyframes shake{0%,100%{transform:translateX(-50%)}25%{transform:translateX(-48%)}75%{transform:translateX(-52%)}}
    @keyframes su{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:translateY(0)}}
    @keyframes tka{0%{transform:translateX(0)}100%{transform:translateX(-50%)}}
  </style>
</head>
<body>
<div class="app" id="appEl">

<!-- TOAST -->
<div class="toast" id="toastMsg"></div>

<!-- DRAWER -->
<div class="ov" id="dov">
  <div class="drawer">
    <div class="dh"><span class="dh-t">☰ MENU</span><span class="dh-x" id="dcl">✕</span></div>
    <div class="db">
      <div class="ds"><div class="ds-t">wallet</div>
        <div class="di" id="wdBtn"><div class="di-l"><span class="di-ic">💸</span><div><div class="di-lb">Withdraw</div><div class="di-sb">instant cashout</div></div></div><span>›</span></div>
        <div class="di"><div class="di-l"><span class="di-ic">📋</span><div><div class="di-lb">Bet History</div></div></div><span>›</span></div>
      </div>
      <div class="ds"><div class="ds-t">preferences</div>
        <div style="background:#0f172a;border-radius:14px;padding:12px">
          <div class="sr"><span class="sl-lb">🔆 Bright</span><input type="range" id="brSlider" min="40" max="100" value="100"><span id="brVal" style="color:#94a3b8;width:36px">100%</span></div>
          <div class="sr"><span class="sl-lb">🔊 Volume</span><input type="range" id="volSlider" min="0" max="100" value="80"><span id="volVal" style="color:#94a3b8;width:36px">80%</span></div>
        </div>
      </div>
      <div class="ds"><div class="ds-t">settings</div>
        <div class="di" style="cursor:default"><div class="di-l"><span class="di-ic">🎵</span><div><div class="di-lb">Sound FX</div></div></div><div class="ts on" id="sndTgl"></div></div>
        <div class="di" style="cursor:default"><div class="di-l"><span class="di-ic">📳</span><div><div class="di-lb">Vibration</div></div></div><div class="ts on" id="vibTgl"></div></div>
      </div>
    </div>
  </div>
</div>

<!-- MODAL -->
<div class="mo" id="dmo">
  <div class="md">
    <div class="mh"><span class="mh-t" id="modTitle">💰 Wallet</span><span class="mh-x" id="mcl">✕</span></div>
    <div class="mb">
      <div class="mt"><div class="mt-b active" id="depTab" onclick="swTab('dep')">Deposit</div><div class="mt-b" id="wdTab" onclick="swTab('wd')">Withdraw</div></div>
      <div class="mbal"><small>Balance</small><br><span id="modBal">0.00 KES</span></div>
      <div id="depForm">
        <label class="mlb">Amount (KES)</label><input class="mi" id="depAmt" type="number" placeholder="min 10" min="10">
        <div class="qd"><div class="qdb" onclick="setDA(100)">100</div><div class="qdb" onclick="setDA(500)">500</div><div class="qdb" onclick="setDA(1000)">1k</div><div class="qdb" onclick="setDA(5000)">5k</div></div>
        <button class="mab dep" onclick="doDep()">✅ Deposit</button>
      </div>
      <div id="wdForm" style="display:none">
        <label class="mlb">Amount</label><input class="mi" id="wdAmt" type="number" placeholder="min 10">
        <label class="mlb">M-Pesa Number</label><input class="mi" id="mpesaPhone" placeholder="07XXXXXXXX" value="0712345678">
        <button class="mab wd" onclick="doWd()">💸 Withdraw to M-Pesa</button>
        <div style="margin-top:10px;color:#94a3b8;font-size:10px;text-align:center">You will receive confirmation SMS</div>
      </div>
    </div>
  </div>
</div>

<!-- HEADER -->
<div class="th">
  <div class="logo">✈ AVIATOR</div>
  <div class="hr">
    <div class="bp" id="depBtn"><span class="ba" id="balAmt">0.00</span><span class="bc">KES</span><span class="dep-tag">+ DEPOSIT</span></div>
    <span class="hi" id="menuBtn">☰</span>
  </div>
</div>

<div class="sb"><div class="fb"><span>🎰</span><span id="stLbl">READY</span></div><span class="bfl">✈ FLY HIGH</span><div class="arb">›</div></div>

<div class="svt" id="srvTabs">
  <div class="st active" data-srv="classic"><span class="sdo" style="background:#3b82f6"></span>Classic</div>
  <div class="st" data-srv="turbo"><span class="sdo" style="background:#f97316"></span>Turbo</div>
  <div class="st" data-srv="moon"><span class="sdo" style="background:#a78bfa"></span>Moon <span style="font-size:8px;color:#f97316;">(1000 KES)</span></div>
</div>

<div class="hs" id="hst"></div>

<div class="ga" id="gameArea">
  <canvas id="gc"></canvas>
  <div class="md-disp" id="mdsp">1.00x</div>
  <div class="wo" id="wov" style="display:none"><div class="wl">next round</div><div class="cd" id="cde">5</div></div>
</div>

<div class="tk"><div class="tki" id="tkInner"></div></div>

<div class="pb">
  <div><span class="ulb">UFC</span> <span style="color:#ef4444;font-weight:900;">✈</span></div>
  <div class="sp"><span>🟢</span><div><div class="spn">SPRIBE</div><div style="color:#4ade80;font-size:9px;">official</div></div></div>
</div>

<div class="pf" id="pFeed"></div>

<!-- BET PANEL -->
<div class="bpn">
  <div class="mto"><div class="mtob active" id="mBet">Manual</div><div class="mtob" id="mAuto">Auto</div></div>
  <div class="bss" id="betSlots">
    <div class="bsl">
      <div class="smr"><div class="smto"><div class="smb active">Bet</div></div></div>
      <div class="sir"><button class="mb2" data-s="0">−</button><input class="ai" id="ba0" type="number" value="10.00" min="10"><button class="pb2" data-s="0">+</button></div>
      <div class="qa"><div class="qa-b" data-s="0" data-v="100">100</div><div class="qa-b sel" data-s="0" data-v="250">250</div><div class="qa-b" data-s="0" data-v="1000">1k</div><div class="qa-b" data-s="0" data-v="25000">25k</div></div>
      <button class="bmb bet-st" id="mb0"><span id="bl0">Bet</span><span class="bsub" id="bs0">10.00 KES</span></button>
      <div style="margin-top:8px; display:none;" id="continueSlot0">
        <button class="qa-b" style="background:#1e3a8a;color:#fff;width:100%;" onclick="cashoutAndContinue(0)">💰 Cashout & Continue</button>
      </div>
    </div>
    <div class="bsl" id="sl1">
      <div class="smr"><div class="smto"><div class="smb active">Bet</div></div><div class="srm" id="rmSl">−</div></div>
      <div class="sir"><button class="mb2" data-s="1">−</button><input class="ai" id="ba1" type="number" value="10.00"><button class="pb2" data-s="1">+</button></div>
      <div class="qa"><div class="qa-b" data-s="1" data-v="100">100</div><div class="qa-b" data-s="1" data-v="250">250</div><div class="qa-b" data-s="1" data-v="1000">1k</div><div class="qa-b" data-s="1" data-v="25000">25k</div></div>
      <button class="bmb bet-st" id="mb1"><span id="bl1">Bet</span><span class="bsub" id="bs1">10.00 KES</span></button>
      <div style="margin-top:8px; display:none;" id="continueSlot1">
        <button class="qa-b" style="background:#1e3a8a;color:#fff;width:100%;" onclick="cashoutAndContinue(1)">💰 Cashout & Continue</button>
      </div>
    </div>
  </div>
</div>
</div>

<script>
(function(){
  let ac, masterVol=0.8, sndOn=true, vibOn=true;
  function gAC(){if(!ac)ac=new(window.AudioContext||window.webkitAudioContext)();return ac;}
  function beep(f,d,t='sine',v=.3){if(!sndOn)return;try{const a=gAC(),o=a.createOscillator(),g=a.createGain();o.type=t;o.frequency.value=f;g.gain.setValueAtTime(v*masterVol,a.currentTime);g.gain.exponentialRampToValueAtTime(.001,a.currentTime+d);o.connect(g);g.connect(a.destination);o.start();o.stop(a.currentTime+d);}catch(e){}}
  function playCO(){[800,1050,1250].forEach((f,i)=>setTimeout(()=>beep(f,.09,'sine',.35),i*60));}
  function playCrash(){if(!sndOn)return;try{const a=gAC(),b=a.createBuffer(1,a.sampleRate*.4,a.sampleRate),d=b.getChannelData(0);for(let i=0;i<d.length;i++)d[i]=(Math.random()*2-1)*Math.pow(1-i/d.length,2);const s=a.createBufferSource();s.buffer=b;const g=a.createGain();g.gain.value=masterVol*.45;s.connect(g);g.connect(a.destination);s.start();}catch(e){}}
  function playBet(){beep(520,.06,'square',.2);}
  function playCd(){beep(380,.06,'sine',.25);}
  function playGo(){[500,750,950].forEach((f,i)=>setTimeout(()=>beep(f,.08,'sine',.3),i*70));}
  function vib(p){if(vibOn)navigator.vibrate?.(p);}

  const SRV={
    classic:{bg:'#0b1320',curve:'#3b82f6',glow:'#1e3a8a40',K:.000048, gen(){const r=Math.random(); if(r<.32)return 1+r/.32*.6; if(r<.55)return 1.6+Math.random()*1.2; if(r<.73)return 2.8+Math.random()*3; if(r<.87)return 5.8+Math.random()*8; if(r<.96)return 13+Math.random()*30; return 43+Math.random()*100;}},
    turbo:{bg:'#0c0a15',curve:'#f97316',glow:'#9a341240',K:.00012, gen(){const r=Math.random(); if(r<.48)return 1+Math.random()*.22; if(r<.68)return 1.22+Math.random()*.5; if(r<.81)return 1.7+Math.random()*1.8; if(r<.91)return 3.5+Math.random()*7; if(r<.97)return 10.5+Math.random()*25; return 35+Math.random()*150;}},
    moon:{bg:'#05070c',curve:'#a78bfa',glow:'#6d28d940',K:.000032, gen(){const r=Math.random(); if(r<.15)return 1+Math.random()*.18; if(r<.28)return 1.18+Math.random()*.6; if(r<.45)return 4+Math.random()*8; if(r<.63)return 12+Math.random()*25; if(r<.78)return 37+Math.random()*70; if(r<.90)return 107+Math.random()*180; return 287+Math.random()*500;}}
  };
  let curSrv='classic', cfg=SRV.classic;
  const canvas=document.getElementById('gc'), ctx=canvas.getContext('2d');
  const mdsp=document.getElementById('mdsp'), wov=document.getElementById('wov'), cde=document.getElementById('cde');
  const balAmt=document.getElementById('balAmt'), stLbl=document.getElementById('stLbl'), hst=document.getElementById('hst'), pFeed=document.getElementById('pFeed');
  let balance=2500.00, mult=1, crashPt=2.5, phase='waiting', afr=null, flStart=null, pts=[];
  const hist={classic:[2.34,1.87,5.21,8.44],turbo:[1.12,1.05,2.34,1.89],moon:[55.2,112.7,8.9,1.3]};
  const SLOTS=[{i:0,state:'idle',bet:10,autoAt:0,auto:false},{i:1,state:'idle',bet:10,autoAt:0,auto:false}];
  const NAMES=['Ace','Mwangi','SkyHigh','Lucky','Pro99','Star','Jet','TurboX','Moon','Nova','King','Flash'];

  function showToast(msg, type='success'){
    const t=document.getElementById('toastMsg'); t.textContent=msg; t.className=`toast ${type} show`;
    setTimeout(()=>t.classList.remove('show'),3000);
  }

  function rsz(){const r=canvas.parentElement.getBoundingClientRect(); const dpr=window.devicePixelRatio||1; canvas.width=r.width*dpr; canvas.height=230*dpr; ctx.setTransform(dpr,0,0,dpr,0,0);}
  function draw(){
    const W=canvas.width/dpr, H=230, PL=35, PB=22, PT=10, PW=W-PL-8, PH=H-PT-PB;
    ctx.fillStyle=cfg.bg; ctx.fillRect(0,0,W,H);
    ctx.strokeStyle='#1e293b'; ctx.lineWidth=.6;
    for(let i=0;i<=4;i++){const y=PT+(i/4)*PH; ctx.beginPath(); ctx.moveTo(PL,y); ctx.lineTo(W-5,y); ctx.stroke();}
    if(curSrv==='moon'){ctx.fillStyle='#cbd5e180'; for(let i=0;i<24;i++){ctx.beginPath(); ctx.arc((i*73+Date.now()*.01)%W, PT+10+(i*31)%PH, i%3?1:1.8,0,7); ctx.fill();}}
    if(pts.length>1){
      ctx.beginPath(); ctx.moveTo(PL,PT+PH); pts.forEach(p=>ctx.lineTo(p.x,p.y)); ctx.lineTo(pts[pts.length-1].x,PT+PH); ctx.closePath();
      const grad=ctx.createLinearGradient(0,PT,0,PT+PH); grad.addColorStop(0,cfg.glow); grad.addColorStop(1,'transparent'); ctx.fillStyle=grad; ctx.fill();
      ctx.beginPath(); pts.forEach((p,i)=>i?ctx.lineTo(p.x,p.y):ctx.moveTo(p.x,p.y));
      ctx.strokeStyle=phase==='crashed'?'#ef4444':cfg.curve; ctx.lineWidth=2.8; ctx.shadowBlur=12; ctx.shadowColor=phase==='crashed'?'#ef4444':cfg.curve; ctx.stroke(); ctx.shadowBlur=0;
    }
    const tip=pts.length?pts[pts.length-1]:{x:PL,y:PT+PH};
    if(phase!=='waiting'){
      ctx.save(); ctx.translate(tip.x,tip.y);
      if(phase==='crashed'){ctx.fillStyle='#fbbf24'; for(let k=0;k<6;k++){ctx.beginPath(); ctx.arc(0,0,8+k*4,0,7); ctx.fillStyle=`rgba(239,68,68,${.6-k*.1})`; ctx.fill();}}
      else {ctx.fillStyle='#fde68a'; ctx.beginPath(); ctx.moveTo(-10,-4); ctx.lineTo(-30+Math.sin(Date.now()*.02)*3,0); ctx.lineTo(-10,4); ctx.fill();}
      ctx.restore();
    }
  }

  function mFromE(e){return Math.exp(cfg.K*e);}
  function eForCP(cp){return Math.log(cp)/cfg.K;}
  function loop(ts){
    if(!flStart)flStart=ts;
    const e=ts-flStart; mult=mFromE(e);
    const W=canvas.width/dpr, H=230, PL=35, PH=H-32; const td=eForCP(crashPt),t=Math.min(e/td,1);
    const px=PL+(W-PL-10)*t, yf=Math.min((mult-1)/(crashPt-1+.001),1), py=10+PH-yf*PH*.88;
    pts.push({x:px,y:py}); if(pts.length>500)pts=pts.slice(-500);
    mdsp.textContent=mult.toFixed(2)+'x';
    SLOTS.forEach(sl=>{if(sl.state==='flying'){document.getElementById('bs'+sl.i).textContent=(sl.bet*mult).toFixed(2)+' KES';}});
    draw();
    if(mult>=crashPt){doCrash();return;}
    afr=requestAnimationFrame(loop);
  }

  function doCrash(){
    phase='crashed'; cancelAnimationFrame(afr); afr=null; mdsp.className='md-disp crashed';
    stLbl.textContent='CRASHED @ '+mult.toFixed(2)+'x'; playCrash(); vib([200,100,300]); draw();
    SLOTS.forEach(sl=>{if(sl.state==='flying'){sl.state='idle'; updSlot(sl); addFeed(`<span class="fn">You</span> lost <span class="fa">${sl.bet.toFixed(0)} KES</span> 💀`);}});
    addHist(mult); setTimeout(startWait,3500);
  }

  function startWait(){
    phase='waiting'; pts=[]; flStart=null; mult=1; crashPt=cfg.gen();
    mdsp.textContent='1.00x'; mdsp.className='md-disp'; wov.style.display='flex'; stLbl.textContent='NEXT ROUND'; draw();
    let cd=5; cde.textContent=cd; playCd();
    const iv=setInterval(()=>{cd--; cde.textContent=cd; playCd(); if(cd<=0){clearInterval(iv); startFlight();}},1000);
  }

  function startFlight(){
    phase='flying'; wov.style.display='none'; mdsp.className='md-disp flying'; stLbl.textContent='FLYING 🚀'; playGo(); vib([40]);
    SLOTS.forEach(sl=>{if(sl.state==='pending'){sl.state='flying'; balance-=sl.bet; updBal(); updSlot(sl);}});
    afr=requestAnimationFrame(loop);
  }

  function cashout(sl){
    if(sl.state!=='flying')return;
    const win=sl.bet*mult; balance+=win; updBal(); sl.state='idle'; playCO(); vib([70,30,70]); updSlot(sl);
    addFeed(`<span class="fn">You</span> won <span class="fw">${win.toFixed(2)} KES</span> @ <span class="fm">${mult.toFixed(2)}x</span> ✅`);
  }

  function cashoutAndContinue(idx){
    const sl=SLOTS[idx]; if(sl.state!=='flying')return;
    const win=sl.bet*mult; balance+=win; updBal(); playCO(); vib([70,30,70]);
    addFeed(`<span class="fn">You</span> cashed & continue <span class="fw">${win.toFixed(2)} KES</span> @ ${mult.toFixed(2)}x`);
    // Place same bet automatically
    const betAmt=sl.bet;
    if(balance<betAmt){showToast('Insufficient balance for continue','warning'); sl.state='idle'; updSlot(sl); return;}
    sl.state='pending'; sl.bet=betAmt; updSlot(sl);
    if(phase==='flying'){sl.state='flying'; balance-=betAmt; updBal(); updSlot(sl);}
    else sl.state='pending';
    showToast(`Continuing with ${betAmt} KES bet`, 'success');
  }

  function slotClick(sl){
    if(sl.state==='idle'){
      const a=parseFloat(document.getElementById('ba'+sl.i).value)||10; if(a<10)return; if(a>balance){showToast('Insufficient balance','warning');return;}
      sl.bet=a;
      if(phase==='flying'){sl.state='flying'; balance-=sl.bet; updBal();}else sl.state='pending';
      playBet(); vib([25]); updSlot(sl);
    }else if(sl.state==='pending'){sl.state='idle'; updSlot(sl);}
    else if(sl.state==='flying')cashout(sl);
  }

  function updSlot(sl){
    const lb=document.getElementById('bl'+sl.i), sub=document.getElementById('bs'+sl.i), btn=document.getElementById('mb'+sl.i), inp=document.getElementById('ba'+sl.i);
    const contDiv=document.getElementById('continueSlot'+sl.i);
    if(sl.state==='idle'){btn.className='bmb bet-st'; lb.textContent='Bet'; sub.textContent=sl.bet.toFixed(2)+' KES'; inp.disabled=false; if(contDiv)contDiv.style.display='none';}
    else if(sl.state==='pending'){btn.className='bmb can-st'; lb.textContent='Cancel'; sub.textContent=sl.bet.toFixed(2)+' KES'; inp.disabled=true; if(contDiv)contDiv.style.display='none';}
    else if(sl.state==='flying'){btn.className='bmb co-st'; lb.textContent='Cash Out'; sub.textContent=(sl.bet*mult).toFixed(2)+' KES'; inp.disabled=true; if(contDiv)contDiv.style.display='block';}
  }

  function addHist(v){hist[curSrv].unshift(v); if(hist[curSrv].length>20)hist[curSrv].pop(); rendHist();}
  function hCol(v){if(v>=50)return 'p'; if(v>=10)return 'o'; if(v>=2)return 'b'; if(v>=1.5)return 'g'; return 'r';}
  function rendHist(){const arr=(hist[curSrv]||[]).slice(0,10); hst.innerHTML=arr.map(v=>`<span class="hv ${hCol(v)}">${v>=100?v.toFixed(0):v>=10?v.toFixed(1):v.toFixed(2)}x</span>`).join('')+'<span class="hm">···</span>';}

  function addFeed(html){const el=document.createElement('div'); el.className='fr'; el.innerHTML=html; pFeed.insertBefore(el,pFeed.firstChild); while(pFeed.children.length>5)pFeed.removeChild(pFeed.lastChild);}
  function updBal(){balAmt.textContent=balance.toFixed(2); document.getElementById('modBal').textContent=balance.toFixed(2)+' KES';}

  window.swTab=(t)=>{const d=t==='dep'; document.getElementById('depTab').className='mt-b'+(d?' active':''); document.getElementById('wdTab').className='mt-b'+(d?'':' active'); document.getElementById('depForm').style.display=d?'block':'none'; document.getElementById('wdForm').style.display=d?'none':'block';};
  window.setDA=(v)=>document.getElementById('depAmt').value=v;
  window.doDep=()=>{const a=parseFloat(document.getElementById('depAmt').value); if(!a||a<10){showToast('Min 10 KES','warning');return;} balance+=a; updBal(); document.getElementById('dmo').classList.remove('open'); addFeed(`<span class="fw">✅ Deposited ${a.toLocaleString()} KES</span>`); playCO(); vib([80]); showToast(`+${a} KES added`);};

  window.doWd=()=>{
    const amt=parseFloat(document.getElementById('wdAmt').value); const phone=document.getElementById('mpesaPhone').value.trim();
    if(!amt||amt<10){showToast('Min 10 KES','warning');return;}
    if(amt>balance){showToast('Insufficient balance','warning');return;}
    if(!phone.match(/^07\d{8}$/)){showToast('Enter valid M-Pesa number (07XXXXXXXX)','warning');return;}
    balance-=amt; updBal(); document.getElementById('dmo').classList.remove('open');
    const code='MP'+Math.random().toString(36).substring(2,10).toUpperCase();
    addFeed(`<span class="fa">💸 Withdrew ${amt} KES to ${phone}</span>`);
    showToast(`✅ M-Pesa: ${amt} KES sent to ${phone}. Conf: ${code}`, 'success');
    vib([150]);
  };

  function swSrv(s){
    if(phase==='flying'){showToast('Wait for round end','warning'); return;}
    if(s==='moon'){
      if(balance<1000){showToast('Moon server requires 1000 KES entry fee','warning'); return;}
      balance-=1000; updBal(); addFeed(`<span class="fa">🌙 Moon entry fee -1000 KES</span>`);
      showToast('🌙 -1000 KES entry fee deducted. Welcome to Moon!', 'success');
    }
    curSrv=s; cfg=SRV[s]; document.querySelectorAll('.st').forEach(t=>t.classList.toggle('active',t.dataset.srv===s));
    rendHist(); draw();
  }

  document.querySelectorAll('.st').forEach(t=>t.addEventListener('click',()=>swSrv(t.dataset.srv)));
  document.getElementById('menuBtn').addEventListener('click',()=>document.getElementById('dov').classList.add('open'));
  document.getElementById('dcl').addEventListener('click',()=>document.getElementById('dov').classList.remove('open'));
  document.getElementById('depBtn').addEventListener('click',()=>{updBal(); swTab('dep'); document.getElementById('dmo').classList.add('open');});
  document.getElementById('wdBtn').addEventListener('click',()=>{updBal(); swTab('wd'); document.getElementById('dmo').classList.add('open'); document.getElementById('dov').classList.remove('open');});
  document.getElementById('mcl').addEventListener('click',()=>document.getElementById('dmo').classList.remove('open'));
  document.getElementById('sndTgl').addEventListener('click',function(){sndOn=!sndOn; this.classList.toggle('on',sndOn);});
  document.getElementById('vibTgl').addEventListener('click',function(){vibOn=!vibOn; this.classList.toggle('on',vibOn);});
  document.getElementById('brSlider').addEventListener('input',function(){document.getElementById('appEl').style.filter=`brightness(${this.value}%)`; document.getElementById('brVal').textContent=this.value+'%';});
  document.getElementById('volSlider').addEventListener('input',function(){masterVol=this.value/100; document.getElementById('volVal').textContent=this.value+'%';});

  SLOTS.forEach(sl=>{
    document.getElementById('mb'+sl.i).addEventListener('click',()=>slotClick(sl));
    document.getElementById('ba'+sl.i).addEventListener('input',e=>{document.getElementById('bs'+sl.i).textContent=(parseFloat(e.target.value)||0).toFixed(2)+' KES';});
  });
  document.querySelectorAll('.mb2').forEach(b=>b.addEventListener('click',()=>{const i=b.dataset.s,inp=document.getElementById('ba'+i); if(SLOTS[i].state!=='idle')return; inp.value=Math.max(10,(parseFloat(inp.value)||10)-10).toFixed(2); document.getElementById('bs'+i).textContent=inp.value+' KES';}));
  document.querySelectorAll('.pb2').forEach(b=>b.addEventListener('click',()=>{const i=b.dataset.s,inp=document.getElementById('ba'+i); if(SLOTS[i].state!=='idle')return; inp.value=((parseFloat(inp.value)||0)+10).toFixed(2); document.getElementById('bs'+i).textContent=inp.value+' KES';}));
  document.querySelectorAll('.qa-b').forEach(el=>el.addEventListener('click',()=>{const si=el.dataset.s,v=parseFloat(el.dataset.v); if(SLOTS[si].state!=='idle')return; document.getElementById('ba'+si).value=v.toFixed(2); document.getElementById('bs'+si).textContent=v.toFixed(2)+' KES'; document.querySelectorAll(`.qa-b[data-s="${si}"]`).forEach(q=>q.classList.remove('sel')); el.classList.add('sel'); SLOTS[si].bet=v;}));
  document.getElementById('rmSl').addEventListener('click',()=>{const s=document.getElementById('sl1'); s.style.display=s.style.display==='none'?'block':'none';});

  function buildTicker(){const items=[]; for(let i=0;i<12;i++){const nm=NAMES[i%NAMES.length],m=(1.2+Math.random()*40).toFixed(2),a=Math.floor(Math.random()*40+1)*100,w=Math.floor(a*parseFloat(m)); items.push(`<span class="tki-i"><span class="tn">${nm}</span> won <span class="tw">${w} KES</span> <span class="tm">@${m}x</span></span>`);} document.getElementById('tkInner').innerHTML=items.join('')+items.join('');}
  function startFP(){setInterval(()=>{if(phase!=='flying')return; const nm=NAMES[Math.floor(Math.random()*NAMES.length)],r=Math.random(); if(r<.4){addFeed(`<span class="fn">${nm}</span> bet <span class="fa">${Math.floor(Math.random()*49+1)*100} KES</span>`);}else if(r<.8){const cm=(1.1+Math.random()*Math.min(mult-.05,mult*.85)).toFixed(2); addFeed(`<span class="fn">${nm}</span> won <span class="fw">${Math.floor(Math.random()*39+1)*100*cm} KES</span> <span class="fm">@${cm}x</span>`);}},900);}

  rsz(); window.addEventListener('resize',()=>{rsz(); draw();});
  updBal(); rendHist(); buildTicker(); startFP(); crashPt=cfg.gen(); draw(); startWait();
})();
</script>
</body>
</html>
